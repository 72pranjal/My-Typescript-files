let userinput:unknown;
let username:string;
userinput=2;
userinput='Tom';
if(typeof userinput==='string'){
username=userinput;
console.log(username);
}

// never type

function num(n:number){
    // console.log(n);
    throw(number:n);
}
console.log(num(4));
